{
  "name": "traveller-monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/backend",
    "packages/frontend/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "turbo": "latest",
    "prettier": "latest"
  },
  "packageManager": "npm@10.0.0"
}
